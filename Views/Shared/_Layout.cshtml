<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - POS Restoran</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-100">
    <div class="flex h-screen">
        <!-- Sidebar -->
        <div class="w-20 bg-blue-900 text-white flex flex-col items-center py-4 space-y-8">
            <!-- Home -->
            <a href="@Url.Action("Index", "Home")"
               class="p-3 rounded-lg hover:bg-blue-800 transition-colors @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Home" ? "bg-blue-800" : "")">
                <i class="fas fa-home text-xl"></i>
            </a>

            <!-- Product Management -->
            <a href="@Url.Action("Index", "Product")"
               class="p-3 rounded-lg hover:bg-blue-800 transition-colors @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Menu" ? "bg-blue-800" : "")">
                <i class="fas fa-box text-xl"></i>
            </a>

            <!-- Dashboard -->
            <a href="@Url.Action("Index", "Dashboard")"
               class="p-3 rounded-lg hover:bg-blue-800 transition-colors @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Dashboard" ? "bg-blue-800" : "")">
                <i class="fas fa-chart-bar text-xl"></i>
            </a>

            <!-- Settings -->
            <a href="@Url.Action("Index", "Settings")"
               class="p-3 rounded-lg hover:bg-blue-800 transition-colors @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Settings" ? "bg-blue-800" : "")">
                <i class="fas fa-cog text-xl"></i>
            </a>
        </div>

        <!-- Main Content -->
        <div class="flex-1 overflow-hidden">
            @RenderBody()
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>